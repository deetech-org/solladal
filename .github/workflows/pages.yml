name: Deploy PWA to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:        # "manually publish" from the Actions tab

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      # Stage ONLY the runtime web assets into ./www (index.html, manifest.json,
      # sw.js, css, js, data, assets) — keeps android/ios/scripts/*.md off the public site.
      - name: Stage web assets
        run: npm run prep:mobile

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./www
      - id: deployment
        uses: actions/deploy-pages@v4
